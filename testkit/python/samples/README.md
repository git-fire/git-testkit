## Python sample smoke implementations

These scripts are runnable examples that exercise the bridge API end-to-end and
act as smoke verification flows.

Run from repo root:

- `python3 testkit/python/samples/smoke_repo_flow.py`
- `python3 testkit/python/samples/smoke_snapshot_flow.py`

They exit non-zero on failure.
